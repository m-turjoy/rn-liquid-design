import styled from 'styled-components'
import {
  fontSize,
  fontFamily,
  color
} from 'styled-system'

const SocialShareWrapper = styled.View``

const IconLabelWrapper = styled.View``

const Label = styled.Text`
  ${fontSize}
  ${fontFamily}
  ${color}
`

const TouchableWrapper = styled.TouchableWithoutFeedback``

const IconWrapper = styled.View``

export { SocialShareWrapper, IconLabelWrapper, Label, TouchableWrapper, IconWrapper }
