import styled from 'styled-components';
import {
  fontFamily,
  fontWeight,
  fontSize,
  lineHeight,
  color,
} from 'styled-system';

const LabelWrapper = styled.Text`
  ${fontFamily}
  ${fontSize}
  ${fontWeight}
  ${lineHeight}
  ${color}
`;

export { LabelWrapper };
